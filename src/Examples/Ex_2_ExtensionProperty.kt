package Examples

fun instructionsEx2 () {
    println("First, uncomment test code.")
    println("Next, write a read-only extension property with List<T> as the receiver type called 'midpointIndex'.")
    println("The extension property should return the midpoint index for the List.")
    println("If the list has an even number of elements, return the index closest to the end.")

}

//write extension property here